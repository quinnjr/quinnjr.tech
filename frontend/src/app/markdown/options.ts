/**
 * Options enum to be passed to the underlying `pulldown-cmark` parser.
 */
export enum Options {
  EnableTables,
  EnableFootnotes,
  EnableStrikethrough,
  EnableTasklists,
  EnableSmartPunctuation
}
